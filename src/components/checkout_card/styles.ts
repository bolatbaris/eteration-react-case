import type { SxProps, Theme } from "@mui/material"

export const checkoutCardStyles: SxProps<Theme> = {
  padding: 2,
  width: "100%"
}

export const totalPriceTextStyles: SxProps<Theme> = {
  fontWeight: "bold"
}
